name: receipt_image_enhancer_example
description: Demonstrates receipt_image_enhancer usage.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.1.2
  receipt_image_enhancer:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/synthetic_receipt.jpg
