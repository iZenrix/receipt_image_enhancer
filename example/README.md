# Example

Small Flutter app for `receipt_image_enhancer`.

Pick (or load) a receipt photo, choose a preset, enhance, then tap the image
to open it full screen with pinch-zoom.

```bash
cd example
flutter pub get
flutter run
```

`scan` uses PNG output so binary text edges stay sharp.
